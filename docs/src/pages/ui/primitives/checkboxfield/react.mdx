import { CheckboxField, Flex, Button } from '@aws-amplify/ui-react';
import { Example } from '@/components/Example';

The `CheckboxField` form primitive is used to create a toggleable checkbox.

<CheckboxField label={'test'} />
